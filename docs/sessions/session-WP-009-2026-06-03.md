---
layout: default
title: "Session Prompt — WP-009 — 2026-06-03"
---

# Session Prompt — WP-009 — 2026-06-03

> Paste this as the first message to the executing Claude Code session. The session opens execution for [WP-009](../work-packets/WP-009-reference-screenshots.md) — capturing the *Showdown in Ghost Town* reference screenshot library from a public YouTube longplay.

---

## Header

- **WP:** `WP-009`
- **Title:** Reference Screenshot Library (*Showdown in Ghost Town*)
- **Session date:** `2026-06-03`
- **Paired pre-flight:** [pre-flight-WP-009-2026-06-03](../reference/pre-flight-WP-009-2026-06-03.md) — verdict `READY`, signed against commit `11d7d16`. Pre-flight is discretionary for this WP ([pre-flight.md exempts screenshot/reference-image captures](../reference/pre-flight.md#when-pre-flight-is-required)); the artifact exists for audit consistency with WP-007 / WP-008, not as a policy contradiction.
- **Targets generation(s):** Gen 1 (*Showdown in Ghost Town* only) — per [01-VISION §Engine Lineage](../01-VISION.md#engine-lineage-2026-06-finding).
- **Targets disc(s):** Showdown — but the capture source is a **public YouTube longplay**, not a mounted ISO. No disc mount needed; no local game binaries are opened.

## Intent

Capture and commit the WP-009 reference screenshot library: PNG files (Required tier — 16 TGIFILE.ART-backed canonical rooms + 1 `OBJ_DAPHNE_A` sprite anchor) and PNG/WebP files (Stretch tier — up to 21 code-rendered canonical rooms) under `docs/assets/screenshots/showdown-screens/`, with the pre-seeded `index.md` filled in row-by-row per the mandatory 6-column provenance schema (`Preview | Room ID | Source Video | Timestamp | Match Status | Notes`). Close the WP with the two-commit topology per [wp-lifecycle §Phase 3](../reference/wp-lifecycle.md#two-commit-topology-for-code-wps): **Commit A** lands the assets and gallery edits; **Commit B** flips WP-009 to ✅ Done in WORK_INDEX and adds 1–2 teaser thumbnails. Verify Pages renders cleanly post-push.

This unblocks [EC-002](../execution-checklists/EC-002-probe-art-harness.md) pre-flight (decoder visual comparison target) and gives [WP-001](../work-packets/WP-001-ghidra-session.md) Ghidra session + Phase 3 `*_Room.cpp` reimplementation a visual ground truth set for the 21 scripted rooms.

**Explicitly NOT in scope for this session:**

- Phantom, Jinx, or Case File screenshots — each is its own WP.
- Cutscene frames — Bink decoder handles those (Phase 3 / 4).
- UI-only screenshots (inventory screen, options screen).
- Animation-frame sprite captures beyond the single `OBJ_DAPHNE_A` (or substitute) Required-tier anchor.
- Capturing from a mounted Showdown ISO playthrough — public YouTube footage only (see §Execution rules).
- Any Ghidra work, any `TGIFILE.ART` decode, any format-doc edits.
- Authoring a Companion EC mid-session — WP-009 has no EC by design.

## Authority chain (read order)

Read these files top-to-bottom before doing any work:

1. `C:\Users\jjensen\.claude\CLAUDE.md` — operator preferences (tone, ownership, even-handedness, commercial defaults)
2. [docs/01-VISION](../01-VISION.md) — engine-lineage finding, generation discipline
3. [docs/reference/wp-lifecycle](../reference/wp-lifecycle.md) — Phase 3 execution discipline, two-commit topology, §End-to-end operator workflow steps 8–13, and the substantive-docs-only triage path (no branch, no PR; direct-to-`main` with `git push origin main` after each commit)
4. [docs/reference/pre-flight](../reference/pre-flight.md) — the gate template; relevant because §When pre-flight is required is what authorized skipping a *mandatory* pre-flight here (the artifact is discretionary)
5. **Paired pre-flight artifact:** [pre-flight-WP-009-2026-06-03](../reference/pre-flight-WP-009-2026-06-03.md) — scope lock + risk profile + binary identity provenance
6. **WP body:** [WP-009-reference-screenshots](../work-packets/WP-009-reference-screenshots.md) — §Goal (capture rules), §Scope (Required vs Stretch tiers + canonical-first naming + UNMATCHED fallback), §Deliverables (mandatory 6-column provenance schema), §Exit criteria (binary Required-tier gate + Stretch-tier honest-accounting), §Execution (inline operator checklist — your primary working document this session), §Notes (governance + generation discipline + why `docs/assets/` not `tools/samples/`)
7. **Gallery scaffold (pre-seeded — your fill-target):** [docs/assets/screenshots/showdown-screens/index.md](../assets/screenshots/showdown-screens/index.md) — all 37 canonical room IDs already populated as `*(pending capture)*` rows in five area tables (Town & story / Boot Hill / Chase sequences / Minigames / Engine & menu), plus the `OBJ_DAPHNE_A` row in Character sprites and an empty Unmatched captures table. **You are filling cells, not building tables.**
8. **WP-008 §Notes — scripted-room split:** [WP-008-object-ini-catalog §Notes](../work-packets/WP-008-object-ini-catalog.md#notes) — identifies 5 of the 21 code-rendered rooms by name (`ROOM_Main_Menu`, `ROOM_P01_Town_Center`, `ROOM_P12_Saloon`, `ROOM_P30_Horseshoe_Corral`, `ROOM_P32_Pie_Noon`). The remaining 16 are either TGIFILE.ART-backed (Required tier) or code-rendered (Stretch tier) — annotate the `Notes` cell with `Required` or `Stretch` as you capture each, based on whether the decoder would or wouldn't produce a matching background.
9. **WORK_INDEX:** [WORK_INDEX.md](../work-packets/WORK_INDEX.md) — Phase 0 row for WP-009 (status flip target in Commit B), Reference Screenshots section (teaser thumbnail target in Commit B)

## Pre-execution checks

Confirm each before touching files:

- [ ] `git status --short` returns empty
- [ ] HEAD on `main` at or after commit `11d7d16` ("docs: WP-009 — conform to pre-flight.md + wp-lifecycle.md"). If `main` has moved with substantive changes since this prompt was authored, STOP and re-verify the pre-flight scope lock still applies per [wp-lifecycle §Stale READY](../reference/wp-lifecycle.md#phase-2--pre-flight).
- [ ] Paired pre-flight artifact `pre-flight-WP-009-2026-06-03.md` is on `main` with verdict `READY`
- [ ] Paired session prompt (this file) is on `main`
- [ ] **The pre-seeded gallery scaffold is present.** Confirm `docs/assets/screenshots/showdown-screens/index.md` exists and carries all 37 canonical room ID rows + the `OBJ_DAPHNE_A` row + the Unmatched table. If any rows are missing, STOP — the scaffold was supposed to be pre-seeded; rebuilding it mid-session is out of scope for this prompt.
- [ ] Internet access + a browser pointed at a *Showdown in Ghost Town* full-game YouTube longplay at ≥ 720p. Record the URL, channel/uploader, and date — you will paste these into the `index.md` §Source video block before capturing the first frame.
- [ ] Screenshot capture tool available: Windows Snipping Tool, Greenshot, ShareX, or OBS frame export. **Do NOT use the browser's "Save image as" on YouTube's video element** — it captures the poster frame or fails; use a real screen-grab tool.
- [ ] The cached `tools/exes/showdown/object.ini` SHA-256 matches `027928BB0219B9EADFEC1E066278D9BD3521B48B3E2D420DD7FEA69558A41C49` (`Get-FileHash tools\exes\showdown\object.ini -Algorithm SHA256`). This is provenance only — you will not parse this file during execution — but a mismatch means the room ID list embedded in the scaffold may be stale, and you should STOP and regenerate the scaffold before capture.

If any check fails: STOP. Surface the failure; do not start capturing against mismatched state.

## Execution rules

**Scope allowlist** (verbatim from pre-flight §Scope lock — any change to files outside this list is a scope violation):

```
docs/assets/screenshots/showdown-screens/*.png
docs/assets/screenshots/showdown-screens/*.webp
docs/assets/screenshots/showdown-screens/index.md
docs/work-packets/WP-009-reference-screenshots.md   (Commit B only — header close-out)
docs/work-packets/WORK_INDEX.md                     (Commit B only — status flip + teasers)
```

**Allowed but local-only:** none. WP-009 produces no gitignored artifacts. Every captured file is either committed to git (Commit A) or it doesn't exist.

**Forbidden:**

- Captures from a locally-mounted Showdown ISO playthrough — out of scope per the WP's fair-use posture; the YouTube-public-footage source IS the copyright posture. If you find yourself screen-recording the game, STOP — that's a different WP.
- Game data binaries committed to git (`*.exe`, `*.eng`, `*.dat`, `*.iso`, `*.ART`, `*.MMF`, `*.MMA`, `*.MMP`, `*.MMS`, `*.ini`) — copyright; safety-net in `.gitignore`. WP-009 should never stage one of these, but the rule applies as a backstop.
- `engines/scooby/**` — Phase 2 territory.
- `docs/formats/**` — WP-009 locks no format facts.
- `docs/execution-checklists/**` — no EC for WP-009; do not author one.
- `tools/**` — no script changes, no extract commits, no cache edits.
- Phantom / Jinx / Case File captures — each is its own future WP.
- Cutscene frames, UI-only screenshots, animation-frame sprites beyond `OBJ_DAPHNE_A` — all explicitly out of scope.
- Any `.jpg` / `.jpeg` file at any tier — Required tier is PNG only; Stretch tier allows WebP but never lossy JPEG.
- The string `Uncertain` anywhere in `index.md`'s Match Status column — the schema is binary `Canonical` / `Unmatched` / `Gap`; there is no third path.

**Generation discipline (REQUIRED):** every capture and every reference in this session is **Gen 1 Showdown**. Do not cross-canonicalize a Phantom or Jinx frame against a Showdown room ID, even if the visual is similar — different binaries, different (though related) asset graphs.

**Copyright posture:** YouTube source only. The single source URL goes in `index.md` §Source video; per-row Timestamp cells trace to that URL. If a second longplay is genuinely needed (the first one skips a chase sequence), add a second URL block and tag affected rows.

**Commit conventions:**

- Subject lines under ~80 chars, imperative voice, `docs:` prefix
- Body explains the **why** in 1–4 sentences
- **Two-commit topology** per wp-lifecycle §Phase 3 + WP-009 §Execution close-out:
  - **Commit A:** `docs: WP-009 — Showdown reference screenshot library` — assets + `index.md` fills
  - **Commit B:** `docs: close WP-009 — flip to ✅ Done` — WP-009 header close-out + WORK_INDEX flip + teasers
- Direct-to-`main` (substantive docs-only WP — no branch, no PR per the triage path)
- `git push origin main` after each commit (NOT batched; pushing late means the gallery isn't visible on Pages until you do)
- Co-Author footer for AI-paired commits:
  ```
  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  ```

**Top risks (verbatim from pre-flight §Risk review — re-stated as imperative):**

- You MUST verify the longplay video title contains "Showdown in Ghost Town" before each capture. YouTube auto-plays adjacent videos; capturing from Phantom or Jinx footage and labeling with a Showdown ID is the highest-impact mode of generation conflation.
- You MUST capture from public YouTube footage. If you find yourself mounting an ISO or screen-recording a live playthrough, STOP — that's outside the fair-use posture.
- You MUST name every saved file by reading the canonical ID directly off the scaffold's `Room ID` column — do not type from memory. The schema is binary canonical-or-UNMATCHED; near-canonical names (`ROOM_P21_BootHill.png` when the catalog says `ROOM_P21_Boot_Hill1`) are the failure mode.
- You MUST NOT use the string `Uncertain` in `index.md`. If you can't canonicalize a frame confidently, save it as `UNMATCHED_<descriptive-slug>.{png,webp}`, log it in the Unmatched captures table, and leave the canonical row as `Gap`. The hard-call escape valve is `UNMATCHED_`, not a third status.
- You MUST verify Required-tier coverage before staging Commit A: all 16 TGIFILE.ART-backed rooms either captured or marked `Gap` with an explicit reason; `OBJ_DAPHNE_A` (or substitute) sprite anchor present. The Required tier is a binary gate — the WP cannot flip to ✅ Done without it.
- You MUST use PNG for every Required-tier file. WebP is permitted only on Stretch-tier captures. JPEG is forbidden at every tier.
- You MUST run `git diff --cached --name-only` before EACH commit and confirm the staged set matches the allowlist for that commit (Commit A: only `docs/assets/screenshots/showdown-screens/` paths; Commit B: only the two WORK_INDEX-side files). Anything else = scope violation; `git restore --staged` it.
- You MUST keep the WORK_INDEX teaser at 1–2 thumbnails, not 5+. Bloating the master ledger undoes a prior pass.
- You MUST embed every gallery image with the `<a href="…"><img src="…" width="320"></a>` pattern from WP-009 §Deliverables — inline `![…](…)` markdown with no width constraint blows up page weight.

## Session task

The actual work statement, mapping to WP-009 §Execution:

### 1. Setup (≤ 10 min)

Per WP-009 §Execution → Setup:

- Locate a Showdown longplay at ≥ 720p; paste URL + uploader + date into `index.md` §Source video.
- Identify which 16 rooms are TGIFILE.ART-backed (Required tier) vs which 21 are code-rendered (Stretch tier). The 5 named in [WP-008 §Notes](../work-packets/WP-008-object-ini-catalog.md#notes) (`ROOM_Main_Menu`, `ROOM_P01_Town_Center`, `ROOM_P12_Saloon`, `ROOM_P30_Horseshoe_Corral`, `ROOM_P32_Pie_Noon`) are Stretch tier; the scaffold already flags these in the `Notes` column. The other 16 of the 21 code-rendered set are not enumerated anywhere in the project docs — annotate them as `Stretch` during capture if the visual doesn't match a recognizable pre-rendered background, or as `Required` if it does. The 16-vs-21 split is the WP-008 baseline; perfect tier classification per row is not required at capture time, only the binary capture-or-Gap decision.

### 2. Per-room capture loop (≤ 90 min)

Per WP-009 §Execution → Per-room capture loop. Run through the longplay; for each distinct room:

- Pause on a clean stationary frame per §Goal capture rules.
- Read the canonical ID directly off the scaffold's `Room ID` column. Save the PNG (Required) or PNG/WebP (Stretch) with that exact filename to `docs/assets/screenshots/showdown-screens/`. If no canonical match is confident, save as `UNMATCHED_<slug>.{png,webp}` and add a row to the Unmatched table.
- Replace the row's `*(pending capture)*` cell with the `<a><img width="320"></a>` embed; fill in Timestamp, Match Status (`Canonical` / `Unmatched`; `Gap` only if you're leaving the row uncaptured), and Notes.

### 3. Close-out + Commit A (≤ 15 min)

Per WP-009 §Execution → Close-out, items 1–3:

- Required-tier check: all 16 TGIFILE.ART-backed rooms either captured or `Gap` with reason; `OBJ_DAPHNE_A` (or substitute) present. **If this check fails, STOP — WP cannot close.**
- Stretch-tier check: each of the 21 code-rendered rooms either captured or `Gap`.
- Confirm at least one Required-tier capture is a full-room static background suitable for EC-002 visual comparison.

Then stage Commit A:

- `git add` the captured `.png` / `.webp` files + the `index.md` edits.
- `git diff --cached --name-only` → expect ONLY paths under `docs/assets/screenshots/showdown-screens/`. If anything else appears, `git restore --staged` and re-stage.
- Commit with subject `docs: WP-009 — Showdown reference screenshot library`; body summarizes Required-tier coverage count, sprite anchor, Stretch-tier coverage count, any Unmatched captures.
- `git push origin main`.

### 4. Commit B — close (≤ 10 min)

Per WP-009 §Execution → Commit B:

- Update `WP-009-reference-screenshots.md` header: Status → ✅ Done (2026-06-03), Actual effort, Findings landed in (link to the gallery), Headline answer (one sentence).
- Update `WORK_INDEX.md`: flip WP-009 row to ✅ Done; add 1–2 teaser thumbnails to the Reference Screenshots section using the embed syntax already documented there; update §Current state if the close shifts next-step calculus.
- `git diff --cached --name-only` → expect ONLY `docs/work-packets/WP-009-reference-screenshots.md` + `docs/work-packets/WORK_INDEX.md`.
- Commit with subject `docs: close WP-009 — flip to ✅ Done`.
- `git push origin main`.

### 5. Pages render verification (≤ 5 min, post-push wait)

After GitHub Pages rebuilds (typically 1–2 min post-push):

- Open `https://github.barefootbetters.com/scooby-engine/docs/assets/screenshots/showdown-screens/` in a browser.
- Confirm all embedded images load (no broken-image icons).
- Confirm page weight is reasonable (the `width="320"` embeds keep it light even at 30+ images).
- If anything renders broken (broken images, broken table layout, gallery not at expected URL), ship a third commit: `docs: WP-009 — fix gallery render`. The close stays landed; only the render fix is the new work.

**Outputs this session MUST produce:**

1. ≥ 16 PNG files in `docs/assets/screenshots/showdown-screens/` matching canonical TGIFILE.ART-backed room IDs (e.g. `ROOM_P21_Boot_Hill1.png`).
2. ≥ 1 PNG file at `docs/assets/screenshots/showdown-screens/OBJ_DAPHNE_A.png` (or substitute sprite anchor — note substitution reason in the row's Notes column).
3. Up to 21 PNG/WebP files matching canonical code-rendered room IDs (Stretch tier; partial coverage acceptable with `Gap` rows for the rest).
4. `index.md` filled in: every captured row carries the embed + Timestamp + Match Status + Notes; every uncaptured canonical row is `Gap` with an explicit reason.
5. Optional: rows in the Unmatched captures table for any frames that couldn't be canonicalized.
6. WP-009 header updated to ✅ Done.
7. WORK_INDEX row updated to ✅ Done + 1–2 teaser thumbnails.

**Outputs this session MUST NOT produce (sprawl guard):**

- Phantom / Jinx / Case File captures of any kind — each is its own WP candidate; log as a follow-up if encountered, don't pursue.
- Cutscene frames — Bink decoder territory.
- Animation-frame sprite series beyond the single `OBJ_DAPHNE_A` anchor — log as Phase 4 inventory animation work, don't pursue.
- A Companion EC file at `docs/execution-checklists/EC-009-*.md` — the inline checklist IS the operational checklist; do not promote it.
- Edits to any `docs/formats/*.md` file.
- Edits to `tools/exes/README.md`, `tools/samples/README.md`, or the asset catalog regeneration script — provenance is locked at the pre-flight; do not refresh during this session.

## Close ritual

Already encoded in §Session task steps 3–5 above. The WP-009 §Execution close-out checklist is the authoritative version; this prompt operationalizes it.

After the WP is closed and pushed:

- Report back to operator with: Required-tier capture count, Stretch-tier capture count, any Unmatched captures (and why), any rows left as `Gap` (and why), any anomalies that came up but didn't fit existing scope (these become follow-up WP candidates).
- Confirm Pages render verification result. If anything's broken, name the fix commit explicitly.

If any §Exit criterion was NOT met (most likely: Required-tier coverage gap that the operator can't close in this session): the WP stays 🚧 In Progress. Do not flip to ✅. Document what's open in the WP §Notes, fill in the pre-flight's §Lessons learned with one line on what blocked, and report back honestly.

---

## After the session — operator review

The session prompt is a fixed artifact once execution opens. If the executing session surfaces a need that wasn't in the prompt (a fourth Match Status, a different naming convention, a need to author an EC), the executing session SHOULD:

1. Stop the current sub-task.
2. Report what it needs and why.
3. Wait for operator confirmation before extending scope.

Silent scope expansion is the failure mode this discipline guards against. WP-009 is a low-variance manual capture pass; the prompt's bounds are deliberately tight because the work is well-defined enough not to need mid-session improvisation.
